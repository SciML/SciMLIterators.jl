var documenterSearchIndex = {"docs":
[{"category":"section","location":"#SciMLIterators.jl","page":"Home","text":"Iterator utilities for SciML solution and integrator objects.\n\nThese iterators were previously part of SciMLBase.jl and have been extracted into their own package for modularity.","title":"SciMLIterators.jl"},{"category":"section","location":"#Public-API","page":"Home","text":"","title":"Public API"},{"category":"function","location":"#SciMLIterators.tuples","page":"Home","text":"tuples(integrator::DEIntegrator)\ntuples(sol::AbstractTimeseriesSolution)\n\nReturn (u, t) pairs from a SciML integrator or solution.\n\nArguments\n\nintegrator: A SciMLBase.DEIntegrator. Returns an iterator that mutates the integrator by stepping it and yielding (integrator.u, integrator.t) after each step.\nsol: A SciMLBase.AbstractTimeseriesSolution. Returns an array formed from tuple.(sol.u, sol.t) without mutating the solution.\n\nInterface\n\nThe integrator method follows the SciMLBase.DEIntegrator iterator contract. Its Base.iterate(integrator, state) method must either return nothing or (integrator, next_state) after advancing the integrator. The returned integrator must expose readable u and t fields. tuples neither calls solver internals nor assumes a particular concrete integrator implementation. The solution method requires sol.u and sol.t to have matching iteration lengths.\n\nExamples\n\nsol = solve(prob, Tsit5())\nhistory = tuples(sol)\n\niter = init(prob, Tsit5())\nfor (u, t) in tuples(iter)\n    # stream states as the integrator advances\nend\n\n\n\n\n\n","title":"SciMLIterators.tuples"},{"category":"function","location":"#SciMLIterators.intervals","page":"Home","text":"intervals(integrator::DEIntegrator)\n\nCreate an iterator that steps the integrator and yields (uprev, tprev, u, t) tuples representing each solution interval.\n\nArguments\n\nintegrator: A SciMLBase.DEIntegrator. Iteration mutates this integrator by stepping it forward.\n\nInterface\n\nintegrator must follow the SciMLBase.DEIntegrator iterator contract: Base.iterate(integrator, state) returns nothing when iteration is complete or (integrator, next_state) after advancing. The returned integrator must expose readable uprev, tprev, u, and t fields. This API is intended for solver implementers and analysis tools; callers must treat the input integrator as consumed and should not concurrently step it through another interface.\n\nExamples\n\niter = init(prob, Tsit5())\nfor (uprev, tprev, u, t) in intervals(iter)\n    dt = t - tprev\nend\n\n\n\n\n\n","title":"SciMLIterators.intervals"},{"category":"type","location":"#SciMLIterators.TimeChoiceIterator","page":"Home","text":"TimeChoiceIterator(integrator, ts)\n\nIterator that evaluates an integrator at specified time points and yields (u, t) pairs for each entry of ts.\n\nFields\n\nintegrator: A SciMLBase.DEIntegrator-compatible object. Iteration mutates this integrator when a requested time is ahead of the current integrator time.\nts: A finite collection of requested time points. length(ts) defines the length of the iterator, and ts[state] must return the requested time for each iteration state.\n\nInterface\n\nintegrator must support the SciML integrator operations used for dense output: SciMLBase.isinplace(integrator.sol.prob), SciMLBase.get_tmp_cache(integrator) for in-place problems, SciMLBase.step!(integrator, dt) for forward movement, and callable dense output as integrator(t) or integrator(out, t). Requested times may move backward relative to the current integrator time only when the integrator supports interpolation at that time.\n\nExamples\n\niter = init(prob, Tsit5())\nfor (u, t) in TimeChoiceIterator(iter, 0.0:0.25:1.0)\n    # `u` is the state evaluated at the requested `t`\nend\n\n\n\n\n\n","title":"SciMLIterators.TimeChoiceIterator"}]
}
